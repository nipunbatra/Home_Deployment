This directory contains all the scripts which are put in the server in /etc/rc.local
These scripts are executed at system startup. 
NB: All these scripts must be run in background and their outputs be piped to suitable files, else they may block execution.
